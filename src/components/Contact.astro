<section class="contact">
  <div class="container">
    <div class="contact-section">
      <h2 class="contact-title"><span class="accent">@</span>Contact</h2>
      <ul class="contact-links">
        <li class="contact-item">
          <a href="mailto:abrahamntl@hotmail.com" class="contact-link">
            <span class="contact-arrow">→</span>
            abrahamntl@hotmail.com
          </a>
        </li>
        <li class="contact-item">
          <a href="https://github.com/lagooncode" target="_blank" class="contact-link">
            <span class="contact-arrow">→</span>
            github.com/lagooncode
          </a>
        </li>
        <li class="contact-item">
          <a href="https://linkedin.com/in/abraham-tovar-laguna" target="_blank" class="contact-link">
            <span class="contact-arrow">→</span>
            linkedin.com/in/abraham-tovar-laguna
          </a>
        </li>
      </ul>
    </div>
  </div>
</section>

<style>
  .contact-section {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    padding-top: var(--section-padding-top);
    padding-bottom: var(--section-padding-bottom);
  }

  .contact-title {
    font-weight: 700;
    font-size: var(--text-md);
  }

  .accent {
    color: var(--color-accent);
  }

  .contact-links {
    display: flex;
    flex-direction: column;
  }

  .contact-item {
    border-bottom: 1px solid #e5e7eb;
  }

  .contact-item:first-child {
    border-top: 1px solid #e5e7eb;
  }

  .contact-link {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 1rem 0;
    font-size: var(--text-base);
    font-weight: 300;
    color: var(--color-text);
    position: relative;
    transition: color 0.2s ease, gap 0.2s ease;
  }

  .contact-link:hover {
    color: var(--color-accent);
    gap: 1.2rem;
  }

  .contact-arrow {
    color: var(--color-accent);
    font-size: var(--text-base);
    transition: transform 0.2s ease;
  }

  .contact-link:hover .contact-arrow {
    transform: translateX(4px);
  }
</style>